需求概述：
- 通过邀请链接注册、管理员审批后的用户，默认等级从“租户等级”调整为“编辑等级”

实施步骤：
- 检索邀请注册与管理员审批流程相关代码位置，定位用户等级初始化逻辑
- 确认用户等级枚举/常量定义与默认值的使用点，找出“租户等级”设置入口
- 将审批通过后的默认等级调整为“编辑等级”，保持其他逻辑不变
- 核对涉及的数据库字段或接口返回结构，确保变更一致且不引入权限缺口
- 运行项目的 lint 与 typecheck（如有脚本），并补充必要的基础验证
