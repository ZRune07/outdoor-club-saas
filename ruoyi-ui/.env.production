# 页面标题
VITE_APP_TITLE = 户外俱乐部SaaS管理平台

# 生产环境配置
VITE_APP_ENV = 'production'

# API基础路径 - 部署时通过环境变量覆盖
VITE_APP_BASE_API = '/prod-api'

# 启用 Mock 数据（生产环境后端不可达时使用 mock 兜底）
VITE_MOCK = true

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip
